const style = (theme) => ({
  PageContainer: {},
  header: {
    display: 'flex',
    alignItems: 'center',
    height: theme.spacing(14),
    padding: theme.spacing(0, 5),
  },
})

export default style
